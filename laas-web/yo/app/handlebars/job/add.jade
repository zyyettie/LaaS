.ui.centered.grid
    .twelve.wide.column
        .ui.segment(style="margin-top:5px;padding:20px 40px;")
            h2#title Creating Job
            form.ui.form
                .two.fields
                    .field
                        label Job Name
                        input(type="text",name="name",placeholder="name", value="{{name}}")
                    .field
                        label Job Scenario
                        select.ui.dropdown(name="selectedScenario")
                            option(value="{{selectedid}}") {{selectedname}}
                            |{{#each scenarioList}}
                            option(value="{{id}}") {{name}}
                            |{{/each}}
                .field
                    label Job Description
                    textarea(type="text",rows="3",name="description",placeholder="description") {{description}}
                #parameters
                .field
                .two.fields
                    .field
                        label Files
                    .field
                        .ui.teal.large.fluid.button#add_file Add
                .field
                    .ui.divided.list
                        |{{#each files}}
                        .item
                            .content
                                {{originalName}}
                        |{{/each}}
                .two.fields(style="margin-top:100px;margin-bottom:70px;")
                    .field(style="padding-right:50px;")
                         .ui.teal.large.fluid.button#job_save Save
                    .field(style="padding-left:50px;")
                         .ui.teal.large.fluid.button#job_run Save & Run
