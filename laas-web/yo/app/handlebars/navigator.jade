#navigator.ui.left.fluid.teal.inverted.vertical.menu
    .item
        .header Service Manager
        .menu
            a.item(href="/jobnew") Create Job
            a.item(href="#") Run Job
            a.item(href="/tasks") Popular Jobs
            .item(style="margin-top:10px;")
                .ui.icon.input
                    input(type="text",placeholder="Search Jobs",style="border-radius:14px;")
                    i.search.link.icon
    .item
        .header My Dashboard
        .menu
            a.item(href="#") My Log Files
            a.item(href="#") My Jobs
            a.item(href="#") Running History
            a.active.item(href="#") Inbox
                .ui.blue.pointing.left.label 1
    .item
        .header Administration

